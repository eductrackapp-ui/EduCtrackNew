<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Couleur de fond (gris clair) -->
    <solid android:color="#FFFFFF"/>

    <!-- Bordure -->
    <stroke
        android:width="2dp"
        android:color="#CCCCCC"/>

    <!-- Ombre douce (optionnelle selon version Android) -->
    <padding
        android:left="2dp"
        android:top="2dp"
        android:right="2dp"
        android:bottom="2dp"/>
</shape>
